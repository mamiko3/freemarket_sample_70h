.card-register
  .register-area
    .register-area__box
      = image_tag "logo.png", alt: "FURIMAロゴ", class: "furima-logo"
      %h1.card 登録クレジットカード情報
      %hr
      %br
        %p.card
          = "**** **** **** " + @default_card_information.last4
          %label.card /有効期限/
          - exp_month = @default_card_information.exp_month.to_s
          - exp_year = @default_card_information.exp_year.to_s.slice(2,3)
          = exp_month + " / " + exp_year
      = form_with url: delete_cards_path, method: :post, id: 'charge-form', html: {name: "inputForm"}, local: true do |form|
        = form.hidden_field value: "" 
        %button.delete 削除する

-# 遷移できるか要確認
.new__content__footer 
  .new__content__footer__list
    %ul
      %li
        %br/
        =link_to "FURIMA トップへ戻る", root_path
      %li
        %br/
        =link_to "マイページ", user_path(current_user)
      %li
        %br/
        =link_to "登録カード情報を見る", card_path
